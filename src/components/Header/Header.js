// @flow
import React, { Component } from 'react';

type Props = {
};

class Header extends Component<Props> {
  render() {
    return (
      <div>
        Header!
      </div>
    );
  }
}

export default Header;
